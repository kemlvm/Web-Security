<section class="page-image page-image-contact md-padding">
  <h1 class="text-white text-center">URANUSFLIX | Post</h1>
</section>

<section id="blog" class="md-padding">
  <div class="container">
    <div class="row">
      <main id="main" class="col-md-8">

        <div class="blog">
          <div class="blog-img">
            <img class="img-fluid" src="{{post.post_image}}" alt="" />
          </div>
          <div class="blog-content">
            <ul class="blog-meta">
              <li><i class="fas fa-user"></i>{{post.author.username}}</li>
              <li><i class="fas fa-clock"></i>{{generateDate
                post.date
                "MM DD YYYY"
                }}</li>
            </ul>
            <h3>{{post.title}}</h3>
            <p>{{post.content}}</p>
            <div class="mb-2"><a href="{{post.link}}">{{post.link_title}}</a></div>
            <div class="mb-2"><a href="{{post.link2}}">{{post.link_title2}}</a></div>


          </div>
        </div>
        <div class="blog-comments">
          
<!--
        {{#if displayLink}}
        <h3>Yorumlar</h3>
        {{#each (limit comment 5)}}
          <div class="media" value="{{_id}}">
            <div class="media-body">
              <h4 class="media-heading">{{name}}</h4>
              <p>{{content}}</p>
            </div>
          </div>
          {{/each}}
          {{/if}}


          {{#unless displayLink}}
          <h2 >Yorumları Götüntülemek İçin Giriş Yapınız</h2>
          <h3 >Yorum Atmak Herkese Serbesttir! Gereksiz Yorumlardan Kurtulmak İçin Kullanıcılarımıza Özel Yaptık</h3>
          {{/unless}}

          
        </div>


        
        <form method="POST" enctype="multipart/form-data" action="">
          <div class="row">
            <div class="col-md-6">
              <div class="form-group">
                <input class="form-control mb-4" name="name" placeholder="Adınızı Giriniz" required="required" />
                <p class="help-block text-danger"></p>
              </div>
              <div class="form-group">
                <input class="form-control mb-4" name="email" type="email" placeholder="Email Giriniz" required="required" />
                <p class="help-block text-danger"></p>
              </div>
            </div>
            <div class="col-md-6">
              <div class="form-group">
                <textarea class="form-control mb-4" name="content" row="5" required="required"
                  placeholder="Görüşünüzü Belirtiniz..."></textarea>
                <p class="help-block text-danger"></p>
              </div>
            </div>
            <div class="clearfix"></div>
            <h3>Yorumun Onaylanma Durumu</h3>
            <input readonly class="form-control mb-4" name="comment_status" value="unapproved">
            <div class="col-lg-12 text-center">
              <div id="success"></div>
              <button id="sendMessageButton" class="main-btn" type="submit">Yorumu Gönder</button>
            </div>
          </div>
        </form>-->

      </main>

      <aside id="aside" class="col-md-4">

        {{> site-sidebar}}

      </aside>

    </div>

  </div>
</section>